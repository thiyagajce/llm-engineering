# Datasets

This folder contains example datasets used by the example projects.

- `sentiment.csv` — small example CSV with `text` and `label`.
- `docs/` — small text documents useful for RAG examples.
